#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(rename_all = "PascalCase")]
pub struct Placement {
  // pub constraints: Constraints,
  // pub preferences: Vec<Spread>,
  // pub platforms: Vec<Platform>,
}
